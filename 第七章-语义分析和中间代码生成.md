## 第一部分 **“中间语言” (Intermediate Representation, IR)**

### 1.1 逆波兰式 (Reverse Polish Notation, RPN)

这个你在数据结构课里应该见过，也叫**后缀表示法**。我们在上一题里生成的 `3 inttoreal 5.5 +` 其实就是这个。

*   **定义**：操作符写在操作数的后面。
*   **特点**：
    1.  **不需要括号**：运算优先级通过位置天然确定。
    2.  **适合栈运算**：读到数字压栈，读到操作符就弹出两个数算一下。
*   **例子**：
    *   中缀：`(a + b) * (c + d)`
    *   后缀：`a b + c d + *`
*   **编译器中的地位**：
    古老的、简单的解释器喜欢用它。但现代高级编译器（如 GCC, LLVM）很少用它做核心 IR，因为它太线性了，不好做优化（比如不好提取循环不变式）。

---

### 1.2 图表示法 (Graphical Representation)

这里主要区分两个概念：**抽象语法树 (AST)** 和 **有向无环图 (DAG)**。

#### (1) 抽象语法树 (AST)
*   **逻辑**：忠实还原句子的语法嵌套结构。
*   **问题**：如果有重复的子表达式，树上会长出两颗一模一样的子树，浪费空间，也不利于发现重复计算。
*   **例子**：`a * b + a * b`
    AST 会有两个 `*` 节点，下面分别挂着 `a` 和 `b`。

#### (2) 有向无环图 (DAG)
*   **逻辑**：**节点去重**。
*   **特点**：如果有子表达式一模一样（比如上面的 `a * b`），DAG 只会画一个节点，然后父节点们都指过来。
*   **作用**：这是编译优化（公共子表达式消除）的第一步。编译器看到 DAG 上某个节点有多个父节点，就知道：“嘿，这块算一次就行了，别傻傻算两次”。

---

### 1.3 三地址代码 (Three-Address Code, TAC) —— **核心重点**

这是本章**最重要**、考试**最爱考**的形式。

**为什么叫“三地址”？**
因为它的一条指令最多包含三个操作数（地址）：`x = y op z`。
*   两个源操作数（y, z）
*   一个目的操作数（x）

它非常接近机器指令（汇编语言通常也是二地址或三地址的），所以从这里转机器码很容易。

三地址代码只是一个**逻辑概念**，它在计算机内存里具体怎么存？有三种常见的数据结构：**四元式、三元式、间接三元式**。

#### (1) 四元式 (Quadruples) —— **最通用、最常考**
它就像一张 Excel 表格，每一行有四列：
`op` (操作符), `arg1` (源1), `arg2` (源2), `result` (结果)

*   **特点**：结果存在显式的**临时变量**（如 `T1`, `T2`）里。
*   **例子**：`a = b * c + d`
    1.  `(*, b, c, T1)`  -> 算出 b*c 存入 T1
    2.  `(+, T1, d, T2)` -> 算出 T1+d 存入 T2
    3.  `(=, T2, _, a)`  -> 把 T2 赋值给 a (赋值号通常只用两个位置)

*   **优点**：**指令可以随便挪动**。因为结果存在 `T1` 这种独立变量里，如果你想把第一行移到后面去，不影响别人找 `T1`。这对代码优化非常重要。

#### (2) 三元式 (Triples)
为了省空间，不想造 `T1`, `T2` 这种临时变量名，于是去掉了 `result` 这一列。
那结果去哪了？**结果就代表“这一行”本身**。

每一行有三列：`op`, `arg1`, `arg2`

*   **引用方式**：使用**行号**（括号括起来）来代表结果。
*   **例子**：`a = b * c + d`
    *   (0) `(*, b, c)`
    *   (1) `(+, (0), d)`  -> 这里的 (0) 指的是第0行的运算结果
    *   (2) `(=, a, (1))`  -> 把第1行的结果赋给 a

*   **缺点**：**致命伤**——**指令不能挪动**。
    如果你把第 (0) 行挪走了，原来的第 (0) 行变成了别的东西，那后面引用 `(0)` 的代码全错了。这导致很难做代码优化。

#### (3) 间接三元式 (Indirect Triples)
为了解决三元式“不能挪动”的问题，引入了一个**“指针表”**。

*   **结构**：
    1.  **三元式表**：存具体的指令，不动它。
    2.  **执行表**：存指令的顺序（指针）。
*   **逻辑**：
    如果要调整代码执行顺序，只调整**执行表**里的指针顺序，不动**三元式表**里的内容。这样行号就不会乱了。
*   *注：这个考得少，知道是为了解决三元式移动困难的问题即可。*

---

### 第一部分 总结与对比（复习小抄）

| 形式 | 核心特征 | 优点 | 缺点 |
| :--- | :--- | :--- | :--- |
| **逆波兰式** | `a b +` | 简单，适合解释器 | 难以优化，表达力弱 |
| **四元式** | `(op, arg1, arg2, res)` | **显式临时变量(T1)**，指令可任意重排，**优化最方便** | 占用空间稍大（因为要存变量名） |
| **三元式** | `(op, arg1, arg2)` | 引用**行号**，省空间 | **强依赖位置**，几乎无法重排优化 |
| **间接三元式** | 列表 + 三元式 | 解决了移动问题 | 两次查找，结构复杂 |

**考试技巧：**
如果题目让你写“三地址代码”或者“中间代码”，**默认指的就是四元式**。除非题目明确要求“写出三元式”。

---

## **2. 说明语句 (Declarations)**

### 2.1 过程中的说明语句（单层）

这是最简单的情况，比如C语言函数内部的一连串变量声明。
```c
int a;
float b;
char c;
```

#### 核心任务：计算偏移量 (Offset)
编译器需要知道每个变量在内存里占多大地方，以及它相对于起点的**相对地址**（偏移量）。

*   **变量 `offset`**：这是一个全局计数器，初始为 0。
*   **处理流程**：
    1.  看到 `int a`：
        *   查知 `int` 宽度为 4。
        *   在符号表中登记：`{ name: "a", type: int, offset: 0 }`。
        *   更新计数器：`offset = offset + 4`。
    2.  看到 `float b`：
        *   查知 `float` 宽度为 8。
        *   在符号表中登记：`{ name: "b", type: float, offset: 4 }`。
        *   更新计数器：`offset = offset + 8`。
    3.  ...以此类推。

**一句话总结**：单层说明语句就是一边读声明，一边累加 `offset`，并把信息填入符号表的过程。

---

### 2.2 保留作用域信息（嵌套）

现在情况复杂了，比如 Pascal 或 C 语言里的**代码块嵌套**：

```c
void P() {
    int x;       // 外层 x
    void Q() {
        int x;   // 内层 x，遮挡了外层
    }
}
```

这里有两个 `x`，编译器不能搞混。这就需要**多层符号表**。

#### (1) 嵌套过程的符号表组织
通常使用 **符号表栈 (Stack of Symbol Tables)**。

*   **进入**一个过程/代码块时：创建一个**新的**符号表，并把它**压栈**。
    *   新的声明都在这个栈顶的表里登记。
    *   如果在这个表里找不到某个变量，就往栈下面（外层表）找。
*   **退出**一个过程/代码块时：把栈顶的符号表**弹出**。
    *   （意味着局部变量销毁/不可见了）。

#### (2) 带嵌套过程的说明语句的翻译
这里涉及到几个经典的语义动作函数（PPT里常考）：

1.  `mktable(previous)`：创建一个新表，并指向它的父表（previous）。
2.  `enter(table, name, type, offset)`：把变量登记到指定的表中。
3.  `addwidth(table, width)`：计算该表所有变量的总宽度（给以后分配栈帧大小用）。
4.  `enterproc(table, name, newtable)`：这是关键！在外层表中登记这个过程名，并把它的指针指向它自己的那张内层表。

**翻译逻辑**：
*   遇到 `Procedure P` -> `t = mktable(top)` (建新表) -> `push(t)` (压栈)。
*   遇到变量声明 -> `enter(top, ...)` (登记在栈顶表)。
*   过程结束 -> `addwidth(...)` -> `pop()` (出栈)。

---

### 2.3 记录中的域名 (Fields in Records / Structs)

这指的是 C 语言中的 `struct` 或 Pascal 中的 `record`。

```c
struct MyData {
    int x;
    float y;
};
struct MyData data;
```

#### 核心逻辑
结构体内部其实就是一个**独立的、迷你的“单层说明语句”环境**。

1.  当编译器读到 `struct MyData` 时，它会暂时把当前的 `offset` **清零**（或者开启一个新的相对计数器）。
2.  `x` 的偏移是 0。
3.  `y` 的偏移是 4（假设 int 占 4）。
4.  这个结构体定义结束后，编译器计算出它的总宽度（比如 4+8=12）。
5.  最后，当声明 `data` 变量时，`data` 的类型就是这个结构体，宽度就是 12。

**注意点**：
结构体内部的变量名（域）是可以和外部变量重名的，因为它们存在于一个独立的命名空间里（通常也是一张独立的各种符号表，挂在结构体类型定义上）。

---

### 第二部分 总结（复习小抄）

这一节如果出题，通常不会让你写复杂的代码，而是考察**符号表的逻辑**：

1.  **Offset**：一定要知道它是怎么累加的。
    *   `当前变量地址 = current_offset`
    *   `next_offset = current_offset + width`
2.  **Scope (作用域)**：
    *   **进入**过程 = 建新表 (`mktable`) + 压栈。
    *   **退出**过程 = 算总宽 + 弹栈。
    *   **查找**变量 = 从栈顶往栈底找（最近匹配原则）。
3.  **结构体**：是具有独立 offset 计算空间的“微型环境”。

---
## **3.赋值语句的翻译**

### 3.1 简单语句及赋值语句的翻译

这是最基础的形态：`x = y + z`。

*   **文法**：$S \to id := E$
*   **逻辑**：
    1.  **查表**：拿着 `id` 去符号表里查，找到它的入口（也就是它在内存里的代表），记为 `p`。
    2.  **拿值**：$E$ 经过之前的计算，结果已经存在 $E.place$ 里了（可能是一个临时变量 `T1`）。
    3.  **生成**：生成一条赋值指令。
*   **生成的四元式**：
    `(=, E.place, _, p)`
    *(意思：把 E 的值给 p)*

---

### 3.2 数组元素的引用 (Array Reference) —— **重难点**

这是考试中出现概率最高的部分。
难点在于：**内存是一维的（一条线），但数组通常是多维的（矩阵）。**
编译器需要生成代码，把多维下标 `[i][j]` 换算成一维的内存偏移量。

#### (1) 地址计算公式 (Row-Major / 按行存储)

绝大多数语言（C, Pascal, Java）都是**按行存储**。意思是先存第一行，再存第二行...

假设数组 `A[d1][d2]...[dn]`，每个元素占 `w` 个字节。
我们要访问 `A[i1][i2]...[in]`。

*   **一维数组** `A[i]`：
    $$Addr = base + i \times w$$
*   **二维数组** `A[i][j]` (列宽为 $n_2$)：
    $$Addr = base + (i \times n_2 + j) \times w$$
    *理解：先跳过 i 整行（每行 $n_2$ 个），再跳过 j 个元素。*
*   **k维数组** (递归逻辑)：
    $$Addr = base + (\dots((i_1 \times n_2 + i_2) \times n_3 + i_3) \dots ) \times w$$

#### (2) 编译器的处理动作 (变址取数)

编译器遇到 `x = A[i][j]` 时，它不知道 `i` 和 `j` 具体等于几（运行时才知道），所以它必须**生成一段计算地址的代码**。

我们引入两个操作符来表示数组的存取（这是四元式里的惯例）：
1.  **取数 `=[]`**：`x = y[k]`  $\to$  `(=[], y, k, x)`
    *   *意思：把数组 y 偏移 k 处的值赋给 x。*
2.  **存数 `[]=`**：`y[k] = x`  $\to$  `([]=, y, k, x)`
    *   *意思：把 x 赋给数组 y 偏移 k 处的位置。*

#### (3) 实战演练：翻译 `A[i][j]`

假设 `A` 是 $10 \times 20$ 的整数数组 (`w=4`)。
代码：`x = A[i][j]`

**步骤拆解（编译器生成的中间代码）：**

1.  **算行偏移**：
    `(*, i, 20, T1)`   // T1 = i * 20 (跳过的完整行数)
2.  **加列偏移**：
    `(+ , T1, j, T2)`  // T2 = i * 20 + j (总的元素个数)
3.  **乘字节宽**：
    `(*, T2, 4, T3)`   // T3 = 总偏移量 (字节数)
4.  **取值**：
    `(=[], A, T3, x)`  // x = A[base + T3]

**考试技巧**：
如果题目让你写四元式，千万别直接写 `x = A[i][j]`。一定要把上面的 `*`, `+`, `*` 这种计算偏移量的过程展开写出来！**“偏移量计算”本身就是代码生成的一部分。**

*(注：有的教材优化了公式，把常数部分 `base` 提前算好，把 `i*w` 这种变数部分留给运行时。但原理都是一样的：算偏移 -> 取数。)*

---
### 3.3 内情向量
#### 1. 什么是内情向量 (Dope Vector)？

简单说，它就是数组的**“身份证”**或**“户口本”**。

*   **定义**：它是一个**数据结构**（通常是在内存里的一小块区域），用来描述一个数组的具体属性。
*   **作用**：当数组的维度、大小在编译时无法确定（或者作为参数传递时），编译器就不能把偏移量写死在代码里。编译器会生成代码去**读取这个内情向量**，动态地获取数组信息来计算地址。

#### 2. 内情向量表里存了什么？

一般包含以下信息（根据语言不同略有差异）：

1.  **首地址 (Base Address)**：数组实际数据存储的起始位置。
2.  **维数 (Dimensions)**：它是几维数组？
3.  **类型 (Type)**：每个元素占几个字节 (width)？
4.  **每维的界限 (Bounds)**：
    *   第一维：下界 $L_1$，上界 $U_1$，长度 $D_1$
    *   第二维：下界 $L_2$，上界 $U_2$，长度 $D_2$
    *   ...

#### 3. 结合内情向量的地址计算

这就涉及到教材中经常考的一个概念：**把地址计算分为“可变部分”和“不变部分”**。

##### (1) 通用公式的展开
假设二维数组 $A[i][j]$，下界分别是 $L_1, L_2$，每行长度是 $D_2$，每个元素宽 $w$。
地址公式本来是：
$$Addr = base + ((i - L_1) \times D_2 + (j - L_2)) \times w$$
*(注意：这里减去 $L_1, L_2$ 是因为下标可能不从0开始，比如 Pascal 的 `1..10`)*

我们把它拆开，整理一下：
$$Addr = \underbrace{(i \times D_2 + j) \times w}_{\text{可变部分 (Variable)}} + \underbrace{base - (L_1 \times D_2 + L_2) \times w}_{\text{不变部分 (Constant)}}$$

##### (2) 为什么要拆分？
*   **可变部分**：包含 $i$ 和 $j$，这部分必须在**运行时**，每次访问数组时计算。
*   **不变部分**：包含 $base, L_1, L_2, D_2, w$。这些在数组创建的时候就确定了！

**内情向量的精髓**就在于：
编译器会在创建数组时，直接算出这个**“不变部分”**（虚拟的首地址），把它存进内情向量里（或者存个简化版）。

这样，以后每次访问 `A[i][j]`，计算公式就简化成了：
$$Addr = \text{内情向量里的虚拟基址} + (i \times D_2 + j) \times w$$

#### 4. 考试怎么考？

关于内情向量，通常有两类考题：

1.  **简答题**：问你内情向量是干什么的？
    *   *答：用于存储数组的维数、界限、首地址等信息，主要用于支持动态数组或数组作为过程参数传递时的地址计算。*

2.  **计算题**：给出一个非 0 开始的数组（如 `A[5..10, -2..3]`），让你区分哪部分是编译时能算的（不变部分），哪部分是运行时算的（可变部分）。
    *   *解题关键*：把公式列出来，凡是带 $i, j$ 的归一堆，剩下的归一堆。剩下的那一堆数值，理论上就是存在内情向量里的修正后的基地址。

---

### 3.4 记录中域的引用 (Record Field)

即 `struct` 或 `record` 的成员访问：`p.x`。

这比数组简单得多，因为**结构体的偏移量在编译时就是固定的**（常数）。

*   **逻辑**：
    1.  查符号表，找到结构体 `p` 的基地址 `base`。
    2.  查结构体定义表，找到成员 `x` 的相对偏移 `offset` (比如 4)。
    3.  因为 `4` 是个常数，编译器不需要生成“计算4”的代码，直接生成取数指令即可。
*   **代码形式**：
    如果是指针访问，通常生成：
    `p` 的地址 + `x` 的偏移 = 目标地址。

---

### 第三部分 总结（复习小抄）

这一节的核心在于 **3.2 数组**。

1.  **简单赋值**：`(=, src, _, dest)`。
2.  **数组公式**（按行主序，Row-Major）：
    *   记住口诀：**前面积累的维度 $\times$ 下一维的宽度 + 当前维下标**。
    *   二维：$(i \times \text{列数} + j) \times w$。
3.  **四元式表达**：
    *   你需要显式地生成 `MULT`, `ADD` 指令来算出偏移量 `Offset`。
    *   最后用 `(=[], Base, Offset, Result)` 来取值。

---






