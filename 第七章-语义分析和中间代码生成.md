## 第一部分 **“中间语言” (Intermediate Representation, IR)**

### 1.1 逆波兰式 (Reverse Polish Notation, RPN)

这个你在数据结构课里应该见过，也叫**后缀表示法**。我们在上一题里生成的 `3 inttoreal 5.5 +` 其实就是这个。

*   **定义**：操作符写在操作数的后面。
*   **特点**：
    1.  **不需要括号**：运算优先级通过位置天然确定。
    2.  **适合栈运算**：读到数字压栈，读到操作符就弹出两个数算一下。
*   **例子**：
    *   中缀：`(a + b) * (c + d)`
    *   后缀：`a b + c d + *`
*   **编译器中的地位**：
    古老的、简单的解释器喜欢用它。但现代高级编译器（如 GCC, LLVM）很少用它做核心 IR，因为它太线性了，不好做优化（比如不好提取循环不变式）。

---

### 1.2 图表示法 (Graphical Representation)

这里主要区分两个概念：**抽象语法树 (AST)** 和 **有向无环图 (DAG)**。

#### (1) 抽象语法树 (AST)
*   **逻辑**：忠实还原句子的语法嵌套结构。
*   **问题**：如果有重复的子表达式，树上会长出两颗一模一样的子树，浪费空间，也不利于发现重复计算。
*   **例子**：`a * b + a * b`
    AST 会有两个 `*` 节点，下面分别挂着 `a` 和 `b`。

#### (2) 有向无环图 (DAG)
*   **逻辑**：**节点去重**。
*   **特点**：如果有子表达式一模一样（比如上面的 `a * b`），DAG 只会画一个节点，然后父节点们都指过来。
*   **作用**：这是编译优化（公共子表达式消除）的第一步。编译器看到 DAG 上某个节点有多个父节点，就知道：“嘿，这块算一次就行了，别傻傻算两次”。

---

### 1.3 三地址代码 (Three-Address Code, TAC) —— **核心重点**

这是本章**最重要**、考试**最爱考**的形式。

**为什么叫“三地址”？**
因为它的一条指令最多包含三个操作数（地址）：`x = y op z`。
*   两个源操作数（y, z）
*   一个目的操作数（x）

它非常接近机器指令（汇编语言通常也是二地址或三地址的），所以从这里转机器码很容易。

三地址代码只是一个**逻辑概念**，它在计算机内存里具体怎么存？有三种常见的数据结构：**四元式、三元式、间接三元式**。

#### (1) 四元式 (Quadruples) —— **最通用、最常考**
它就像一张 Excel 表格，每一行有四列：
`op` (操作符), `arg1` (源1), `arg2` (源2), `result` (结果)

*   **特点**：结果存在显式的**临时变量**（如 `T1`, `T2`）里。
*   **例子**：`a = b * c + d`
    1.  `(*, b, c, T1)`  -> 算出 b*c 存入 T1
    2.  `(+, T1, d, T2)` -> 算出 T1+d 存入 T2
    3.  `(=, T2, _, a)`  -> 把 T2 赋值给 a (赋值号通常只用两个位置)

*   **优点**：**指令可以随便挪动**。因为结果存在 `T1` 这种独立变量里，如果你想把第一行移到后面去，不影响别人找 `T1`。这对代码优化非常重要。

#### (2) 三元式 (Triples)
为了省空间，不想造 `T1`, `T2` 这种临时变量名，于是去掉了 `result` 这一列。
那结果去哪了？**结果就代表“这一行”本身**。

每一行有三列：`op`, `arg1`, `arg2`

*   **引用方式**：使用**行号**（括号括起来）来代表结果。
*   **例子**：`a = b * c + d`
    *   (0) `(*, b, c)`
    *   (1) `(+, (0), d)`  -> 这里的 (0) 指的是第0行的运算结果
    *   (2) `(=, a, (1))`  -> 把第1行的结果赋给 a

*   **缺点**：**致命伤**——**指令不能挪动**。
    如果你把第 (0) 行挪走了，原来的第 (0) 行变成了别的东西，那后面引用 `(0)` 的代码全错了。这导致很难做代码优化。

#### (3) 间接三元式 (Indirect Triples)
为了解决三元式“不能挪动”的问题，引入了一个**“指针表”**。

*   **结构**：
    1.  **三元式表**：存具体的指令，不动它。
    2.  **执行表**：存指令的顺序（指针）。
*   **逻辑**：
    如果要调整代码执行顺序，只调整**执行表**里的指针顺序，不动**三元式表**里的内容。这样行号就不会乱了。
*   *注：这个考得少，知道是为了解决三元式移动困难的问题即可。*

---

### 第一部分 总结与对比（复习小抄）

| 形式 | 核心特征 | 优点 | 缺点 |
| :--- | :--- | :--- | :--- |
| **逆波兰式** | `a b +` | 简单，适合解释器 | 难以优化，表达力弱 |
| **四元式** | `(op, arg1, arg2, res)` | **显式临时变量(T1)**，指令可任意重排，**优化最方便** | 占用空间稍大（因为要存变量名） |
| **三元式** | `(op, arg1, arg2)` | 引用**行号**，省空间 | **强依赖位置**，几乎无法重排优化 |
| **间接三元式** | 列表 + 三元式 | 解决了移动问题 | 两次查找，结构复杂 |

**考试技巧：**
如果题目让你写“三地址代码”或者“中间代码”，**默认指的就是四元式**。除非题目明确要求“写出三元式”。

---

## **2. 说明语句 (Declarations)**

### 2.1 过程中的说明语句（单层）

这是最简单的情况，比如C语言函数内部的一连串变量声明。
```c
int a;
float b;
char c;
```

#### 核心任务：计算偏移量 (Offset)
编译器需要知道每个变量在内存里占多大地方，以及它相对于起点的**相对地址**（偏移量）。

*   **变量 `offset`**：这是一个全局计数器，初始为 0。
*   **处理流程**：
    1.  看到 `int a`：
        *   查知 `int` 宽度为 4。
        *   在符号表中登记：`{ name: "a", type: int, offset: 0 }`。
        *   更新计数器：`offset = offset + 4`。
    2.  看到 `float b`：
        *   查知 `float` 宽度为 8。
        *   在符号表中登记：`{ name: "b", type: float, offset: 4 }`。
        *   更新计数器：`offset = offset + 8`。
    3.  ...以此类推。

**一句话总结**：单层说明语句就是一边读声明，一边累加 `offset`，并把信息填入符号表的过程。

---

### 2.2 保留作用域信息（嵌套）

现在情况复杂了，比如 Pascal 或 C 语言里的**代码块嵌套**：

```c
void P() {
    int x;       // 外层 x
    void Q() {
        int x;   // 内层 x，遮挡了外层
    }
}
```

这里有两个 `x`，编译器不能搞混。这就需要**多层符号表**。

#### (1) 嵌套过程的符号表组织
通常使用 **符号表栈 (Stack of Symbol Tables)**。

*   **进入**一个过程/代码块时：创建一个**新的**符号表，并把它**压栈**。
    *   新的声明都在这个栈顶的表里登记。
    *   如果在这个表里找不到某个变量，就往栈下面（外层表）找。
*   **退出**一个过程/代码块时：把栈顶的符号表**弹出**。
    *   （意味着局部变量销毁/不可见了）。

#### (2) 带嵌套过程的说明语句的翻译
这里涉及到几个经典的语义动作函数（PPT里常考）：

1.  `mktable(previous)`：创建一个新表，并指向它的父表（previous）。
2.  `enter(table, name, type, offset)`：把变量登记到指定的表中。
3.  `addwidth(table, width)`：计算该表所有变量的总宽度（给以后分配栈帧大小用）。
4.  `enterproc(table, name, newtable)`：这是关键！在外层表中登记这个过程名，并把它的指针指向它自己的那张内层表。

**翻译逻辑**：
*   遇到 `Procedure P` -> `t = mktable(top)` (建新表) -> `push(t)` (压栈)。
*   遇到变量声明 -> `enter(top, ...)` (登记在栈顶表)。
*   过程结束 -> `addwidth(...)` -> `pop()` (出栈)。

---

### 2.3 记录中的域名 (Fields in Records / Structs)

这指的是 C 语言中的 `struct` 或 Pascal 中的 `record`。

```c
struct MyData {
    int x;
    float y;
};
struct MyData data;
```

#### 核心逻辑
结构体内部其实就是一个**独立的、迷你的“单层说明语句”环境**。

1.  当编译器读到 `struct MyData` 时，它会暂时把当前的 `offset` **清零**（或者开启一个新的相对计数器）。
2.  `x` 的偏移是 0。
3.  `y` 的偏移是 4（假设 int 占 4）。
4.  这个结构体定义结束后，编译器计算出它的总宽度（比如 4+8=12）。
5.  最后，当声明 `data` 变量时，`data` 的类型就是这个结构体，宽度就是 12。

**注意点**：
结构体内部的变量名（域）是可以和外部变量重名的，因为它们存在于一个独立的命名空间里（通常也是一张独立的各种符号表，挂在结构体类型定义上）。

---

### 第二部分 总结（复习小抄）

这一节如果出题，通常不会让你写复杂的代码，而是考察**符号表的逻辑**：

1.  **Offset**：一定要知道它是怎么累加的。
    *   `当前变量地址 = current_offset`
    *   `next_offset = current_offset + width`
2.  **Scope (作用域)**：
    *   **进入**过程 = 建新表 (`mktable`) + 压栈。
    *   **退出**过程 = 算总宽 + 弹栈。
    *   **查找**变量 = 从栈顶往栈底找（最近匹配原则）。
3.  **结构体**：是具有独立 offset 计算空间的“微型环境”。

---
## **3.赋值语句的翻译**

### 3.1 简单语句及赋值语句的翻译

这是最基础的形态：`x = y + z`。

*   **文法**：$S \to id := E$
*   **逻辑**：
    1.  **查表**：拿着 `id` 去符号表里查，找到它的入口（也就是它在内存里的代表），记为 `p`。
    2.  **拿值**：$E$ 经过之前的计算，结果已经存在 $E.place$ 里了（可能是一个临时变量 `T1`）。
    3.  **生成**：生成一条赋值指令。
*   **生成的四元式**：
    `(=, E.place, _, p)`
    *(意思：把 E 的值给 p)*

---

### 3.2 数组元素的引用 (Array Reference) —— **重难点**

这是考试中出现概率最高的部分。
难点在于：**内存是一维的（一条线），但数组通常是多维的（矩阵）。**
编译器需要生成代码，把多维下标 `[i][j]` 换算成一维的内存偏移量。

#### (1) 地址计算公式 (Row-Major / 按行存储)

绝大多数语言（C, Pascal, Java）都是**按行存储**。意思是先存第一行，再存第二行...

假设数组 `A[d1][d2]...[dn]`，每个元素占 `w` 个字节。
我们要访问 `A[i1][i2]...[in]`。

*   **一维数组** `A[i]`：
    $$Addr = base + i \times w$$
*   **二维数组** `A[i][j]` (列宽为 $n_2$)：
    $$Addr = base + (i \times n_2 + j) \times w$$
    *理解：先跳过 i 整行（每行 $n_2$ 个），再跳过 j 个元素。*
*   **k维数组** (递归逻辑)：
    $$Addr = base + (\dots((i_1 \times n_2 + i_2) \times n_3 + i_3) \dots ) \times w$$

#### (2) 编译器的处理动作 (变址取数)

编译器遇到 `x = A[i][j]` 时，它不知道 `i` 和 `j` 具体等于几（运行时才知道），所以它必须**生成一段计算地址的代码**。

我们引入两个操作符来表示数组的存取（这是四元式里的惯例）：
1.  **取数 `=[]`**：`x = y[k]`  $\to$  `(=[], y, k, x)`
    *   *意思：把数组 y 偏移 k 处的值赋给 x。*
2.  **存数 `[]=`**：`y[k] = x`  $\to$  `([]=, y, k, x)`
    *   *意思：把 x 赋给数组 y 偏移 k 处的位置。*

#### (3) 实战演练：翻译 `A[i][j]`

假设 `A` 是 $10 \times 20$ 的整数数组 (`w=4`)。
代码：`x = A[i][j]`

**步骤拆解（编译器生成的中间代码）：**

1.  **算行偏移**：
    `(*, i, 20, T1)`   // T1 = i * 20 (跳过的完整行数)
2.  **加列偏移**：
    `(+ , T1, j, T2)`  // T2 = i * 20 + j (总的元素个数)
3.  **乘字节宽**：
    `(*, T2, 4, T3)`   // T3 = 总偏移量 (字节数)
4.  **取值**：
    `(=[], A, T3, x)`  // x = A[base + T3]

**考试技巧**：
如果题目让你写四元式，千万别直接写 `x = A[i][j]`。一定要把上面的 `*`, `+`, `*` 这种计算偏移量的过程展开写出来！**“偏移量计算”本身就是代码生成的一部分。**

*(注：有的教材优化了公式，把常数部分 `base` 提前算好，把 `i*w` 这种变数部分留给运行时。但原理都是一样的：算偏移 -> 取数。)*

---
### 3.3 内情向量
#### 1. 什么是内情向量 (Dope Vector)？

简单说，它就是数组的**“身份证”**或**“户口本”**。

*   **定义**：它是一个**数据结构**（通常是在内存里的一小块区域），用来描述一个数组的具体属性。
*   **作用**：当数组的维度、大小在编译时无法确定（或者作为参数传递时），编译器就不能把偏移量写死在代码里。编译器会生成代码去**读取这个内情向量**，动态地获取数组信息来计算地址。

#### 2. 内情向量表里存了什么？

一般包含以下信息（根据语言不同略有差异）：

1.  **首地址 (Base Address)**：数组实际数据存储的起始位置。
2.  **维数 (Dimensions)**：它是几维数组？
3.  **类型 (Type)**：每个元素占几个字节 (width)？
4.  **每维的界限 (Bounds)**：
    *   第一维：下界 $L_1$，上界 $U_1$，长度 $D_1$
    *   第二维：下界 $L_2$，上界 $U_2$，长度 $D_2$
    *   ...

#### 3. 结合内情向量的地址计算

这就涉及到教材中经常考的一个概念：**把地址计算分为“可变部分”和“不变部分”**。

##### (1) 通用公式的展开
假设二维数组 $A[i][j]$，下界分别是 $L_1, L_2$，每行长度是 $D_2$，每个元素宽 $w$。
地址公式本来是：
<img width="524" height="42" alt="image" src="https://github.com/user-attachments/assets/83293135-5a10-458f-afdf-6b0289ef78e5" />

*(注意：这里减去 $L_1, L_2$ 是因为下标可能不从0开始，比如 Pascal 的 `1..10`)*

我们把它拆开，整理一下：
<img width="644" height="108" alt="image" src="https://github.com/user-attachments/assets/d1dc09be-6d3f-40ab-8929-c80198137c29" />


##### (2) 为什么要拆分？
*   **可变部分**：包含 $i$ 和 $j$，这部分必须在**运行时**，每次访问数组时计算。
*   **不变部分**：包含 $base, L_1, L_2, D_2, w$。这些在数组创建的时候就确定了！

**内情向量的精髓**就在于：
编译器会在创建数组时，直接算出这个**“不变部分”**（虚拟的首地址），把它存进内情向量里（或者存个简化版）。

这样，以后每次访问 `A[i][j]`，计算公式就简化成了：
<img width="593" height="68" alt="image" src="https://github.com/user-attachments/assets/07dbe349-9af6-41e0-9f5a-901861c30f74" />


#### 4. 考试怎么考？

关于内情向量，通常有两类考题：

1.  **简答题**：问你内情向量是干什么的？
    *   *答：用于存储数组的维数、界限、首地址等信息，主要用于支持动态数组或数组作为过程参数传递时的地址计算。*

2.  **计算题**：给出一个非 0 开始的数组（如 `A[5..10, -2..3]`），让你区分哪部分是编译时能算的（不变部分），哪部分是运行时算的（可变部分）。
    *   *解题关键*：把公式列出来，凡是带 $i, j$ 的归一堆，剩下的归一堆。剩下的那一堆数值，理论上就是存在内情向量里的修正后的基地址。

---

### 3.4 记录中域的引用 (Record Field)

即 `struct` 或 `record` 的成员访问：`p.x`。

这比数组简单得多，因为**结构体的偏移量在编译时就是固定的**（常数）。

*   **逻辑**：
    1.  查符号表，找到结构体 `p` 的基地址 `base`。
    2.  查结构体定义表，找到成员 `x` 的相对偏移 `offset` (比如 4)。
    3.  因为 `4` 是个常数，编译器不需要生成“计算4”的代码，直接生成取数指令即可。
*   **代码形式**：
    如果是指针访问，通常生成：
    `p` 的地址 + `x` 的偏移 = 目标地址。

---

### 第三部分 总结（复习小抄）

这一节的核心在于 **3.2 数组**。

1.  **简单赋值**：`(=, src, _, dest)`。
2.  **数组公式**（按行主序，Row-Major）：
    *   记住口诀：**前面积累的维度 $\times$ 下一维的宽度 + 当前维下标**。
    *   二维：$(i \times \text{列数} + j) \times w$。
3.  **四元式表达**：
    *   你需要显式地生成 `MULT`, `ADD` 指令来算出偏移量 `Offset`。
    *   最后用 `(=[], Base, Offset, Result)` 来取值。

---


## 4.布尔表达式的翻译

在高级语言中，布尔表达式（如 `a > b or c < d`）有两种完全不同的用途，也就对应两种完全不同的翻译方法。

### 4.1 数值表达式法 (Numerical Representation)

这是我们在 C 语言里常见的逻辑：**真就是 1，假就是 0**。
比如 `x = (a > b);`，如果 `a>b`，`x` 就等于 1。

*   **适用场景**：布尔值被用来**赋值**或**计算**时。
*   **翻译逻辑**：需要生成临时变量，把判断结果存进去。
*   **生成的四元式样子**：
    ```text
    1. if a > b goto 4
    2. T1 = 0        (假)
    3. goto 5
    4. T1 = 1        (真)
    5. ...
    ```
    *(你看，为了算个 1 或 0，它其实内部也用了跳转)*

### 4.2 作为条件控制的布尔表达式翻译 (Control Flow) —— **本章核心中的核心**

这是我们在写 `if (a > b)` 或 `while (a > b)` 时的情况。
这里的 `a > b` 不需要产生 1 或 0，它只需要决定：**程序的下一条指令是去执行 `then` 的代码，还是 `else` 的代码**。

这种翻译方法叫 **“真假出口”法**。

---

### 重点概念：真出口与假出口

对于一个布尔表达式 `E`，我们不给它算值，而是给它分配两个**属性**（通常是标号 Label）：
1.  **E.true** (真出口)：如果 E 为真，程序应该跳到哪里去执行。
2.  **E.false** (假出口)：如果 E 为假，程序应该跳到哪里去执行。

我们来看看基本的逻辑运算 `or`, `and`, `not` 是怎么玩弄这两个出口的。

#### (1) `E = E1 or E2` (逻辑或)
*   **短路逻辑**：
    *   如果 `E1` 是真，那 `E` 肯定是真。所以 `E1` 的真出口就是 `E` 的真出口。
    *   如果 `E1` 是假，别急，还得去看看 `E2`。所以 `E1` 的假出口，是 `E2` 的代码开始处。
    *   `E2` 如果真，整个 `E` 就真；`E2` 如果假，整个 `E` 就假。

**图解翻译：**
```text
      if E1 goto E.true   (E1真，直接起飞)
      goto L_E2           (E1假，还得去测E2)
L_E2: if E2 goto E.true   (E2真，也算赢)
      goto E.false        (E2也假，彻底输了)
```

#### (2) `E = E1 and E2` (逻辑与)
*   **短路逻辑**：
    *   如果 `E1` 是假，那 `E` 肯定是假。所以 `E1` 的假出口就是 `E` 的假出口。
    *   如果 `E1` 是真，还不能庆祝，得去测 `E2`。
    *   只有 `E2` 也是真，才是真的真。

**图解翻译：**
```text
      if E1 goto L_E2     (E1真，还得过E2这关)
      goto E.false        (E1假，直接没戏)
L_E2: if E2 goto E.true   (E2真，那才是真)
      goto E.false        (E2假，还是没戏)
```

#### (3) `E = not E1` (逻辑非)
*   **逻辑**：把真假出口**互换**一下就行了。
    *   `E.true` = `E1.false`
    *   `E.false` = `E1.true`
    *   *(不需要生成任何代码，只是编译器内部交换标签)*

---

### 实战演练：翻译 `a < b or c < d and e < f`

假设：
*   整个表达式的真出口是 `L_True`
*   整个表达式的假出口是 `L_False`

**步骤分析**（注意优先级 `and` 高于 `or`）：
表达式结构是：`(a < b) OR (c < d AND e < f)`

1.  **处理 OR 左边 (`a < b`)**：
    *   如果真：直接跳去 `L_True`（不用管右边了）。
    *   如果假：跳去右边的测试代码（我们叫它 `L1`）。
    *   **生成代码**：
        ```text
        if a < b goto L_True
        goto L1
        ```

2.  **处理 OR 右边 (`... AND ...`)**：
    *   现在到了标号 `L1`。
    *   我们要测 `c < d`。
        *   如果假：那就全完了（因为是 AND），跳去 `L_False`。
        *   如果真：跳去测 `e < f`（我们叫它 `L2`）。
    *   **生成代码**：
        ```text
        L1: if c < d goto L2
            goto L_False
        ```

3.  **处理最后一个 (`e < f`)**：
    *   现在到了标号 `L2`。
    *   这是最后一关。真去 `L_True`，假去 `L_False`。
    *   **生成代码**：
        ```text
        L2: if e < f goto L_True
            goto L_False
        ```

**把上面三段拼起来，就是完整的中间代码。**

---
### 4.3 回填

但你有没有发现一个**致命的 bug**？
当我们编译器是**一遍扫描 (One-pass)** 的时候：
1.  读到 `if a < b goto ...` 时，我只读到了第 1 行。
2.  此时，真出口 `L_True` 可能在第 100 行，假出口 `L1` 可能在第 5 行。
3.  **问题来了**：编译器读到第 1 行的时候，根本不知道第 5 行和第 100 行在哪里！**那这个 `goto` 后面填什么数字呢？**

这时候，就需要 **“回填”技术**。

---

#### 1. 什么是回填 (Backpatching)？

用一句话解释：**“先把坑挖好，留个空，等将来知道地址了，再回来把空填上。”**

它的核心思想是：
编译器在生成跳转指令时，不知道跳去哪里，就先生成一个**残缺的四元式**：
`(j<, a, b, 0)`  <-- 最后的 0 是个占位符，表示“还没填”。

同时，编译器会在手里攥着一张**“欠条清单”**（链表），上面写着：“第 1 行指令缺真出口地址”。等将来编译器扫描到了真出口的位置（比如第 100 行），它就拿出这张清单，把第 1 行的那个 `0` 改成 `100`。

---

#### 2. 核心数据结构：三个链表

为了实现回填，我们在布尔表达式 `E` 上挂了三个**属性**，它们不是具体的数值，而是**链表（List）**：

1.  **`E.truelist` (真链)**：
    *   这是一个名单。
    *   记录了所有“如果 `E` 为真，就要跳转”的那些指令的**行号**。
    *   *潜台词：等找到了 `True` 的去处，记得回来通知这帮兄弟。*

2.  **`E.falselist` (假链)**：
    *   这也是一个名单。
    *   记录了所有“如果 `E` 为假，就要跳转”的那些指令的**行号**。

3.  **`nextlist` (下一条链)**（通常用于语句 S）：
    *   记录了所有“执行完 S 后，要跳到 S 后面那条指令”的指令行号。

---

#### 3. 核心操作函数

教材或 PPT 上通常会列出三个函数，必须背下来：

1.  **`makelist(i)`**：
    *   新建一个链表，里面只包含行号 `i`。
    *   *场景：刚生成一条残缺指令 `(j, ..., ..., 0)`，赶紧把当前行号 `i` 记下来。*

2.  **`merge(p1, p2)`**：
    *   把两个链表 `p1` 和 `p2` 合并成一个新链表。
    *   *场景：`E = E1 or E2`。E1 为真要跳去 True，E2 为真也要跳去 True。所以把 `E1.truelist` 和 `E2.truelist` 合并，大家一起等。*

3.  **`backpatch(p, i)`** —— **这就是“回填”动作！**
    *   参数 `p`：一个链表（名单）。
    *   参数 `i`：一个具体的目标地址（行号）。
    *   **动作**：遍历链表 `p` 里的每一个行号，去四元式数组里找到对应的指令，把它们的跳转目标这一栏，全部填成 `i`。

---

#### 4. 举个栗子说明流程

假设我们要翻译 `a < b or c < d`。
四元式数组下标从 `100` 开始。

**步骤 1：处理 `a < b`**
*   生成指令 100：`if a < b goto _`  (真出口未知)
*   生成指令 101：`goto _`         (假出口未知)
*   此时：
    *   `E1.truelist = {100}`
    *   `E1.falselist = {101}`

**步骤 2：处理 `or` (关键！)**
*   遇到 `or`，意味着 `a < b` 如果为假，应该去测 `c < d`。
*   也就是说，`c < d` 的起始位置（即 102 行），就是 `E1` 的假出口！
*   **执行回填**：`backpatch(E1.falselist, 102)`。
*   *结果：指令 101 变成了 `goto 102`。*

**步骤 3：处理 `c < d`**
*   生成指令 102：`if c < d goto _`
*   生成指令 103：`goto _`
*   此时：
    *   `E2.truelist = {102}`
    *   `E2.falselist = {103}`

**步骤 4：合并**
*   整个表达式 `E` 的真出口是啥？只要 E1 真 **或者** E2 真都行。
*   `E.truelist = merge(E1.truelist, E2.truelist) = {100, 102}`。
*   `E.falselist = E2.falselist = {103}`。

**最终状态：**
代码生成了，但还有坑没填：
*   100: `if a < b goto ?` (在 truelist 里)
*   101: `goto 102`      (已填)
*   102: `if c < d goto ?` (在 truelist 里)
*   103: `goto ?`        (在 falselist 里)

等到将来这一整句 `if (...)` 解析完了，知道 `then` 在 104 行，`else` 在 200 行，就会调用：
*   `backpatch({100, 102}, 104)`
*   `backpatch({103}, 200)`

---

#### 总结

*   **为什么要回填？** 因为一遍扫描时不知道后面的地址。
*   **怎么回填？** 用链表 (`truelist`, `falselist`) 记账，等到了目的地，再批量填坑 (`backpatch`)。
*   **和下一节的关系**：下一节讲控制语句 (`if`, `while`)，其实就是在确定“目的地”的位置，然后调用 `backpatch` 把布尔表达式留下的坑填上。

---

### 第 4 节 总结（复习小抄）

这一节你不需要记具体的四元式，而是要记住 **“数据流向”**：

1.  **短路计算**是核心。
2.  **OR (`A or B`)**：
    *   A 真 $\to$ 整体真。
    *   A 假 $\to$ 去测 B。
3.  **AND (`A and B`)**：
    *   A 真 $\to$ 去测 B。
    *   A 假 $\to$ 整体假。
4.  **NOT (`not A`)**：
    *   真假互换。

---





