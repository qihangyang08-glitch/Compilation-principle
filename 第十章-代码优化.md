### 第一讲：代码优化的概述与常用技术

这一节的目标是让你看到一段代码，能一眼看出“这里写得太笨了，可以改得更聪明”。

#### 1. 优化概述

*   **什么是代码优化？**
    *   编译器在把中间代码转换成目标代码的过程中，通过一系列变换，让生成的程序**跑得更快**（时间优化）或者**占内存更少**（空间优化）。
    *   *注意*：不能改变程序的运行结果！这是铁律。

*   **优化的三原则**（考试可能问简答）：
    1.  **等价性**：优化前和优化后，程序干的事必须一样。（最重要）
    2.  **有效性**：优化后速度必须变快，或者体积变小。（不然折腾啥？）
    3.  **合算性**：编译的时间不能增加太多。（为了快1秒的运行，编译花了一整天，不划算）。

#### 2. 最常用的优化技术（重点！大题里的每一步就在做这些）

这里有6个核心招式，我会用最简单的例子演示，你一定要记住名字和套路。

**招式一：删除公共子表达式 (Common Subexpression Elimination)**
*   **原理**：如果不止一次计算同一个公式，且中间变量没变过，那就只算一次。
*   **例子**：
    ```c
    x = a + b;
    ...
    y = a + b; // 笨！a和b都没变，为什么重算？
    ```
    **优化后**：
    ```c
    t = a + b;
    x = t;
    ...
    y = t; // 直接用算好的
    ```

**招式二：代码外提 (Code Motion)**
*   **原理**：在循环里，有些计算每次循环结果都一样（与循环变量无关），那就把它踢到循环外面去，只算一次。
*   **例子**：
    ```c
    while (i < n) {
        x = y + z; // y和z都不随i变化
        a[i] = 6 * x;
        i++;
    }
    ```
    **优化后**：
    ```c
    t = y + z; // 提到外面，只算1次，而不是n次
    while (i < n) {
        a[i] = 6 * t;
        i++;
    }
    ```

**招式三：强度削弱 (Strength Reduction)**
*   **原理**：把耗时的操作（如乘法、除法）换成省时的操作（如加法、移位）。CPU 做加法比做乘法快得多。
*   **例子**：
    ```c
    // 假设 i 每次增加 1
    y = i * 2;
    ```
    **优化后**：
    ```c
    y = i + i; // 或者 y = i << 1
    ```
    *在循环中更常见：把 `4 * i` 变成 `t = t + 4`。*

**招式四：变换循环控制条件**
*   **原理**：有时候循环判断条件里有复杂的计算，把它简化。
*   **例子**：
    ```c
    // 每次都要算一遍 i <= n-1 吗？
    while (i <= n - 1) { ... }
    ```
    **优化后**：
    ```c
    t = n - 1;
    while (i <= t) { ... }
    ```

**招式五：合并已知量 (Constant Folding)**
*   **原理**：编译时如果已经知道变量是常数，直接算出来。
*   **例子**：
    ```c
    int a = 2 * 3;
    ```
    **优化后**：
    ```c
    int a = 6; // 编译器直接填6，不要留给运行时的CPU去算乘法
    ```

**招式六：复写传播 (Copy Propagation)**
*   **原理**：如果 `x = y`，那么后面用到 `x` 的地方直接用 `y` 代替，这样 `x` 可能就没用了，可以删掉。
*   **例子**：
    ```c
    x = y;
    z = x + 1;
    ```
    **优化后**：
    ```c
    x = y;
    z = y + 1; // 此时如果没有别的地方用x，"x=y"这句就可以删了（死代码删除）
    ```

**招式七：删除无用赋值 (Dead Code Elimination)**
*   **原理**：算了一个变量，结果后面谁也没用到它，或者程序根本执行不到这里，那就直接删掉。

---

### 第一讲小结

这一讲主要解决“有哪些手段”的问题。
**做题时的思路**：当你看到那道10分大题时，你要像个“找茬员”一样扫描代码：
1.  有没有重复算的？（公共子表达式）
2.  有没有循环里不变的？（代码外提）
3.  有没有乘法能变加法的？（强度削弱）
4.  有没有全是常数的？（合并已知量）

---
