### 第一讲：代码优化的概述与常用技术

这一节的目标是让你看到一段代码，能一眼看出“这里写得太笨了，可以改得更聪明”。

#### 1. 优化概述

*   **什么是代码优化？**
    *   编译器在把中间代码转换成目标代码的过程中，通过一系列变换，让生成的程序**跑得更快**（时间优化）或者**占内存更少**（空间优化）。
    *   *注意*：不能改变程序的运行结果！这是铁律。

*   **优化的三原则**（考试可能问简答）：
    1.  **等价性**：优化前和优化后，程序干的事必须一样。（最重要）
    2.  **有效性**：优化后速度必须变快，或者体积变小。（不然折腾啥？）
    3.  **合算性**：编译的时间不能增加太多。（为了快1秒的运行，编译花了一整天，不划算）。

#### 2. 最常用的优化技术（重点！大题里的每一步就在做这些）

这里有6个核心招式，我会用最简单的例子演示，你一定要记住名字和套路。

**招式一：删除公共子表达式 (Common Subexpression Elimination)**
*   **原理**：如果不止一次计算同一个公式，且中间变量没变过，那就只算一次。
*   **例子**：
    ```c
    x = a + b;
    ...
    y = a + b; // 笨！a和b都没变，为什么重算？
    ```
    **优化后**：
    ```c
    t = a + b;
    x = t;
    ...
    y = t; // 直接用算好的
    ```

**招式二：代码外提 (Code Motion)**
*   **原理**：在循环里，有些计算每次循环结果都一样（与循环变量无关），那就把它踢到循环外面去，只算一次。
*   **例子**：
    ```c
    while (i < n) {
        x = y + z; // y和z都不随i变化
        a[i] = 6 * x;
        i++;
    }
    ```
    **优化后**：
    ```c
    t = y + z; // 提到外面，只算1次，而不是n次
    while (i < n) {
        a[i] = 6 * t;
        i++;
    }
    ```

**招式三：强度削弱 (Strength Reduction)**
*   **原理**：把耗时的操作（如乘法、除法）换成省时的操作（如加法、移位）。CPU 做加法比做乘法快得多。
*   **例子**：
    ```c
    // 假设 i 每次增加 1
    y = i * 2;
    ```
    **优化后**：
    ```c
    y = i + i; // 或者 y = i << 1
    ```
    *在循环中更常见：把 `4 * i` 变成 `t = t + 4`。*

**招式四：变换循环控制条件**
*   **原理**：有时候循环判断条件里有复杂的计算，把它简化。
*   **例子**：
    ```c
    // 每次都要算一遍 i <= n-1 吗？
    while (i <= n - 1) { ... }
    ```
    **优化后**：
    ```c
    t = n - 1;
    while (i <= t) { ... }
    ```

**招式五：合并已知量 (Constant Folding)**
*   **原理**：编译时如果已经知道变量是常数，直接算出来。
*   **例子**：
    ```c
    int a = 2 * 3;
    ```
    **优化后**：
    ```c
    int a = 6; // 编译器直接填6，不要留给运行时的CPU去算乘法
    ```

**招式六：复写传播 (Copy Propagation)**
*   **原理**：如果 `x = y`，那么后面用到 `x` 的地方直接用 `y` 代替，这样 `x` 可能就没用了，可以删掉。
*   **例子**：
    ```c
    x = y;
    z = x + 1;
    ```
    **优化后**：
    ```c
    x = y;
    z = y + 1; // 此时如果没有别的地方用x，"x=y"这句就可以删了（死代码删除）
    ```

**招式七：删除无用赋值 (Dead Code Elimination)**
*   **原理**：算了一个变量，结果后面谁也没用到它，或者程序根本执行不到这里，那就直接删掉。

---

### 第一讲小结

这一讲主要解决“有哪些手段”的问题。
**做题时的思路**：当你看到那道10分大题时，你要像个“找茬员”一样扫描代码：
1.  有没有重复算的？（公共子表达式）
2.  有没有循环里不变的？（代码外提）
3.  有没有乘法能变加法的？（强度削弱）
4.  有没有全是常数的？（合并已知量）

---


### 第二讲：局部优化 (Local Optimization)

所谓的“局部”，指的就是**基本块（Basic Block）**。

#### 1. 基本块 (Basic Block) —— 优化的最小单位

*   **定义**：
    一段顺序执行的代码序列。
    *   **入口唯一**：程序只能从第一句进去。
    *   **出口唯一**：只能从最后一句出来。
    *   **中间不能跳**：中间不允许有 `goto` 跳转进来，也不允许中间有 `if/goto` 跳出去。
    *   *比喻*：基本块就像是一根**直通的水管**。一旦水流进来了，就必须流到头，中间既没有分叉也没有汇合。

*   **为什么要切分基本块？**
    因为在基本块内部，代码是绝对按顺序执行的，没有分支判断，编译器可以很大胆地进行优化（比如乱序执行、合并计算），不用担心流程跳来跳去出错。

*   **怎么划分基本块？（考试重点算法）**
    给你一串三地址码（中间代码），怎么切？
    **第一步：找“首领”（Leaders，即每个块的第一句话）**。
    满足下列条件的语句是首领：
    1.  程序的**第一条语句**。
    2.  能被跳转到的语句（即**跳转指令的目标**，比如 `goto L1`，那么 `L1` 标记的那行就是首领）。
    3.  **紧跟在跳转语句后面**的那条语句（因为上一句跳走了，下一句就是一个新块的开始）。

    **第二步：切块**。
    从一个首领开始，直到（但不包括）下一个首领之前的语句，这就构成了一个基本块。

#### 2. 流图 (Flow Graph)

*   把所有切好的“基本块”当作节点。
*   如果块 A 执行完可能会跳到块 B，就在 A 和 B 之间画个箭头。
*   这样画出来的图就是**程序流图**。它宏观地展示了程序的逻辑结构（循环、分支）。

---

#### 3. 基本块的 DAG 表示 (重点中的重点)

这是本章**最核心的技术**，也是局部优化的具体实现手段。
**DAG (Directed Acyclic Graph)** 即“有向无环图”。

*   **为什么用 DAG？**
    DAG 能自动帮你完成两件事：
    1.  **检测公共子表达式**：如果算过一遍 `a+b`，下次再算 `a+b` 时，直接指向之前那个节点，不用新建节点。
    2.  **发现无用代码**：画完图后，有些节点如果没人指向它（也没被标记为活跃变量），那就是废代码，可以直接扔掉。

*   **怎么画 DAG？（手把手教你）**
    假设我们有以下四元式代码：
    1.  `T1 = A + B`
    2.  `T2 = A + B`
    3.  `C = T1 * T2`

    **作图步骤**：
    1.  **叶子节点**：给初始变量 `A` 和 `B` 画圈。
    2.  **处理第一句 `T1 = A + B`**：
        *   画一个操作符节点 `+`。
        *   两个箭头分别从 `A` 和 `B` 指向这个 `+`。
        *   给这个 `+` 节点贴上标签：`T1`。
    3.  **处理第二句 `T2 = A + B`**：
        *   本来想画个新的 `+`，但编译器一看：咦？`A` 和 `B` 之前是不是做过 `+` 运算？
        *   发现图里已经有一个 `+` 连着 `A` 和 `B` 了。
        *   **不画新节点！** 直接把标签 `T2` 也贴到之前那个 `T1` 的节点上。
        *   *(你看，公共子表达式 `A+B` 自动被合并了)*
    4.  **处理第三句 `C = T1 * T2`**：
        *   画一个操作符节点 `*`。
        *   左边箭头来自 `T1` 所在的节点，右边箭头来自 `T2` 所在的节点（其实它俩是同一个节点）。
        *   给这个 `*` 贴上标签 `C`。

*   **重组代码**：
    画完图后，我们根据图重新写代码，就变成了：
    `T1 = A + B`
    `C = T1 * T1` (优化完成！)

#### 4. DAG 构造中的细节（坑点）

在画 DAG 时，最容易晕的是**变量的重新赋值**。
*   **原则**：变量标签是可以**移动**的。
*   **例子**：
    ```c
    x = y + z  // 1
    x = x - 1  // 2
    ```
    1.  画 `y`, `z`，画 `+`，把 `x` 贴在 `+` 上。
    2.  画 `-`，连接 `x`（刚才的+号）和常数 `1`。
    3.  现在要把计算结果赋给 `x` 了。**把 `x` 从 `+` 号节点上撕下来，贴到新的 `-` 号节点上。**
    4.  原来的 `+` 号节点如果没别的标签（比如没有中间变量 `t` 引用它），它就变成了“无名氏”，如果它不是根节点，可能就会被当做垃圾处理掉。

---

### 第二讲小结

这一讲你只需要掌握两个技能：
1.  **怎么切**：看到代码，能用“找首领”的方法切成基本块。
2.  **怎么画**：在基本块内部，能画出 DAG。

**DAG 的核心逻辑**就是：
*   **去重**：长得一样的运算只画一个节点。
*   **最新**：变量名永远只贴在它**最新**的值所在的节点上。

---

### 第三讲：循环优化 (Loop Optimization)

#### 3.1 程序流图 vs 基本块 DAG

这一点在考试的概念题里容易混淆，我们先拆解一下：

*   **基本块 DAG（局部）**：
    *   **范围**：只在一个基本块**内部**。
    *   **作用**：看数据怎么流动，为了消除重复计算（公共子表达式）。
    *   *比喻*：你家**客厅**的装修图，看沙发能不能和电视柜共用一面墙。
*   **程序流图 (Flow Graph)（全局）**：
    *   **范围**：把**基本块**当作一个个节点连起来。
    *   **作用**：看控制流怎么跳，为了发现**循环**。
    *   *比喻*：整个**小区**的地图，看哪几栋楼围成了一个圈。

#### 3.2 循环的查找 (Finding Loops)

编译器怎么知道哪几行代码是个循环？它不是看 `while` 关键字，而是看图的结构。

*   **循环的定义（自然循环）**：
    1.  **唯一的入口**：循环外要想进循环，只能从这一个入口（叫**循环头 Header**）进来。
    2.  **回边 (Back Edge)**：有一条路，从循环里面指回了循环头。
    *   *就像一个贪吃蛇咬尾巴的结构。*

---

#### 3.3 三大循环优化技术（重点！大题解题步骤）

这三招经常是**连招**，按顺序使用的。我们用一个具体的例子来演示。

**原始代码**：我们要计算 `y = i * 10`，循环 10 次。
```c
i = 1;
while (i <= 10) {
    x = a + b;   // 假设 a, b 在循环里不变
    y = i * 10;
    i = i + 1;
}
```

**第一招：代码外提 (Code Motion)**

*   **目标**：把循环里**不变**的东西扔出去。
*   **识别**：`x = a + b`。因为 `a` 和 `b` 在循环里没有被重新赋值，所以 `x` 每次算出来都一样。这叫**循环不变量**。
*   **操作**：提！
    ```c
    x = a + b;      // 【移出来了】只算1次
    i = 1;
    while (i <= 10) {
        y = i * 10;
        i = i + 1;
    }
    ```

**第二招：强度削弱 (Strength Reduction)**

*   **目标**：把贵的运算（乘除）变成便宜的（加减）。
*   **识别**：`y = i * 10`。
    *   `i` 每次增加 1。
    *   `y` 是 `i` 的 10 倍。
    *   那 `i` 每加 1，`y` 不就是加 10 吗？
*   **操作**：引入一个新的变量（比如 `t`）来代替乘法。
    ```c
    x = a + b;
    i = 1;
    t = 10;         // t 初始化为 i*10 (即 1*10)
    while (i <= 10) {
        y = t;      // 【乘法没了！】直接用 t
        t = t + 10; // 【强度削弱】用加法代替乘法
        i = i + 1;
    }
    ```

**第三招：删除归纳变量 (Induction Variable Elimination)**

这是最高级的一步，也是强度削弱的后续。

*   **什么是归纳变量？**
    *   如果在循环里，变量 `i` 和变量 `t` 的变化是**步调一致**的（比如 `i` 走一步，`t` 走十步），它俩就构成了线性关系。我们管它俩叫**归纳变量**。
    *   在这里，`i` 是**基本归纳变量**（控制循环次数），`t` 是**族归纳变量**（跟着 `i` 混的）。
*   **目标**：既然 `t` 和 `i` 步调一致，而且 `i` 除了用来计数（判断 `i<=10`）没别的用了，那能不能直接用 `t` 来判断结束？
    *   当 `i = 10` 时，`t = 100`。
    *   所以 `i <= 10` 等价于 `t <= 100`。
*   **操作**：杀掉 `i`，只留 `t`。
    ```c
    x = a + b;
    // i = 1;     // i 不需要了
    t = 10;
    while (t <= 100) {  // 【变换循环控制条件】用 t 来控制
        y = t;
        t = t + 10;
        // i = i + 1; // i 不需要了
    }
    ```

---

### 第三讲小结

这一讲我们学会了如何把一个笨重的循环变成一个精简的循环。
**连招口诀**：
1.  **提**：把不变的计算提到循环外。
2.  **降**：把循环里的乘法（`i * c`）变成加法（`t = t + c`）。
3.  **删**：如果原来的循环变量 `i` 没用了，直接用新的 `t` 来控制循环，把 `i` 删掉。

这套连招，就是解决你那个 C 语言数组题目的标准答案步骤！

