下面我有ppt里对文法二义性本质特征和两种解决方法的描述，但是说得太晦涩了，请你用他这个内容重新讲一下，作为一个知识板块
S → if b S
    | if b S else S
    | A (其他语句)
试证明该文法是二义性的，并消除它。

分析：该文法的句子 if b if b A else A 对应下图中两颗不同的语法树，所以文法是二义性的。
（1）不改变已有规则，仅加进一项非形式的语法规定：

else 与前面最接近的不带 else 的 if 相对应。

这样，文法 G 的句子 **if b if b A else A** 只对应唯一的一棵语法树。
分析引起二义性的原因：
是 if-else 语句的 then 部分可以是 if 型；

改写文法时的考虑：

> 规定 if 和 else 之间只能是
   两分支 if-else 语句 或 其他语句。

> 规定 if 和 else 之间不能是单分支的 if
   因为 if b if b A else A，
   会出现一个else需要在两个 if 中进行选择的问题。

分成两类

1 > 两分支 if-else 语句 和 其他语句，称为匹配句
