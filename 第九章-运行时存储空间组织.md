
### 第一部分：目标程序运行时的活动

这一节其实就在回答一个问题：**写在纸上的静态代码（代码段），变成跑在CPU里的动态程序（进程）时，到底发生了什么变化？**

#### 1.1 过程的活动 (Procedure Activities)

*   **概念**：
    *   **过程（Procedure）**：你可以理解为“函数”或“方法”。在编译器的视角里，它是一段静止的代码。
    *   **活动（Activity/Activation）**：当这个函数被**调用**时，它就“活”了。这就叫一次“活动”。
    *   **生存期（Lifetime）**：从函数第一行代码开始执行，到最后一行代码执行完毕（或者return），这段时间就是这次活动的生存期。

*   **核心难点（也是重点）**：
    **代码是唯一的，但活动可以是无限的。**
    *   *例子*：你写了一个递归函数 `fact(n)` 用来求阶乘。
    *   代码只有一份 `fact` 的定义。
    *   当你调用 `fact(3)` 时，内存里会出现 `fact(3)` 的活动；它又调用 `fact(2)`，内存里接着出现 `fact(2)` 的活动。
    *   这时候，`fact(3)` 还没结束（活着），`fact(2)` 也活着。虽然它们用的是同一份代码，但在内存里是**两个完全独立的实体**。

#### 1.2 参数传递 (Parameter Passing)

当 `Main` 函数调用 `Function A` 时，需要把数据传过去，这就是参数传递。
虽然课本上会列出“传名、传值、传地址”等，你只需要重点理解最核心的两种区别，这直接决定了编译器怎么处理内存：

1.  **传值 (Call by Value)**：
    *   **比喻**：我有一份笔记，复印一份给你。你在复印件上乱涂乱画，**不会影响**我原本的那份。
    *   **编译器视角**：把实际参数的值算出来，**拷贝**到被调用函数的存储区里。
2.  **传引用/传地址 (Call by Reference)**：
    *   **比喻**：我把我家保险柜的**钥匙**给你。你打开拿走里面的钱，我再去开，钱就真的**没了**。
    *   **编译器视角**：把实际参数的**内存地址**传过去。被调用函数直接操作原来的内存区域。

---

### 第二部分：运行时存储器的划分

这一节讲的是：**编译器怎么向操作系统申请内存，并把这块内存切成不同的区域给程序用。**

#### 2.1 运行时的存储器的划分

想象操作系统给你的程序分配了一大块长条形的内存地皮。编译器通常会把它切成四块（这图你肯定见过）：

1.  **代码区 (Code)**：
    *   放编译后的机器指令。
    *   **特点**：大小固定，只读（不许改代码），也就是前面说的“静态的代码”。
2.  **静态数据区 (Static Data)**：
    *   放全局变量、常量（比如 C 里的 `static int a`）。
    *   **特点**：程序刚启动就占好坑了，直到程序结束才释放。大小也是编译时就知道的。
3.  **栈区 (Stack)**：
    *   放**函数调用**产生的数据（比如局部变量）。
    *   **特点**：像叠盘子，函数调用就压上去，函数返回就弹出来。**向低地址生长**（通常画在内存图的上方，往下长）。
4.  **堆区 (Heap)**：
    *   放你手动申请的数据（比如 C里的 `malloc`，Java/C++ 里的 `new`）。
    *   **特点**：杂乱无章，需要程序员或垃圾回收机制来管理。**向高地址生长**（通常画在内存图的下方，往上长）。

> **重点**：栈和堆中间有一块空地，如果栈长得太深（死递归），或者堆申请太多，两者撞上了，就报 `Out of Memory` 或 `Stack Overflow`。

#### 2.2 活动记录 (Activation Record / AR)

这是本章**最核心**的名词！一定要听懂。

*   **是什么？**
    还记得1.1里说的“过程的活动”吗？每次函数被调用，总得有个地方记下这个函数的参数是多少、局部变量是什么、算完后返回到哪里去吧？
    这个**记录所有信息的“小本本”**，就叫**活动记录**（也叫**栈帧** Stack Frame）。

*   **里面装了什么？**（一个典型的活动记录包含以下条目，从上到下）：
    1.  **返回值**：给调用者留的。
    2.  **实参**：调用者传进来的参数。
    3.  **控制链 (Control Link / Dynamic Link)**：指向**调用者**的活动记录的指针。（用来函数返回时恢复现场，比如 B 运行完了，得把栈顶指回 A）。
    4.  **访问链 (Access Link / Static Link)**：用来找非局部变量的（后面讲嵌套过程时是重点，先有个印象）。
    5.  **保存的机器状态**：CPU 寄存器的值（函数切换时要由CPU暂存）。
    6.  **局部变量**：函数内部定义的 `int i` 这种。
    7.  **临时变量**：计算 `(a+b)*(c+d)` 时，`a+b` 的结果需要找个地方暂存一下。

#### 2.3 存储分配策略

根据语言特性的不同，编译器会选择不同的“分蛋糕”策略：

1.  **静态分配 (Static Allocation)**：
    *   **代表**：早期的 Fortran。
    *   **逻辑**：编译的时候就算死了每个变量在哪，不需要栈，也不需要堆。
    *   **缺点**：**不支持递归**（因为递归需要同时存在两个同名函数的局部变量，静态分配只有一个坑，没法放）。
2.  **栈式分配 (Stack Allocation)**：
    *   **代表**：C, Pascal, Java (方法调用部分)。
    *   **逻辑**：也就是我们最常用的。函数调用的进出符合“后进先出”原则，非常适合用栈。
3.  **堆式分配 (Heap Allocation)**：
    *   **代表**：Lisp, Java (对象实例部分)。
    *   **逻辑**：数据的生存期和函数调用无关。比如你函数结束了，但你在函数里 `new` 出来的对象可能还得留着给别人用，这就不能放在栈里（因为栈里的东西函数一结束就弹飞了），只能放堆里。

---

### 第一讲总结（复习一下）

1.  **活动 vs 过程**：过程是死代码，活动是跑起来的一次次调用。
2.  **内存四区**：代码、静态、栈（放函数）、堆（放动态对象）。
3.  **活动记录 (AR)**：就是**栈**里的每一层，里面存了局部变量、参数和返回地址。函数调用一次，就往栈里压一个 AR。

---
