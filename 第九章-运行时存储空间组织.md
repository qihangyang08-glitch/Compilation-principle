
### 第一部分：目标程序运行时的活动

这一节其实就在回答一个问题：**写在纸上的静态代码（代码段），变成跑在CPU里的动态程序（进程）时，到底发生了什么变化？**

#### 1.1 过程的活动 (Procedure Activities)

*   **概念**：
    *   **过程（Procedure）**：你可以理解为“函数”或“方法”。在编译器的视角里，它是一段静止的代码。
    *   **活动（Activity/Activation）**：当这个函数被**调用**时，它就“活”了。这就叫一次“活动”。
    *   **生存期（Lifetime）**：从函数第一行代码开始执行，到最后一行代码执行完毕（或者return），这段时间就是这次活动的生存期。

*   **核心难点（也是重点）**：
    **代码是唯一的，但活动可以是无限的。**
    *   *例子*：你写了一个递归函数 `fact(n)` 用来求阶乘。
    *   代码只有一份 `fact` 的定义。
    *   当你调用 `fact(3)` 时，内存里会出现 `fact(3)` 的活动；它又调用 `fact(2)`，内存里接着出现 `fact(2)` 的活动。
    *   这时候，`fact(3)` 还没结束（活着），`fact(2)` 也活着。虽然它们用的是同一份代码，但在内存里是**两个完全独立的实体**。

#### 1.2 参数传递 (Parameter Passing)

当 `Main` 函数调用 `Function A` 时，需要把数据传过去，这就是参数传递。
虽然课本上会列出“传名、传值、传地址”等，你只需要重点理解最核心的两种区别，这直接决定了编译器怎么处理内存：

1.  **传值 (Call by Value)**：
    *   **比喻**：我有一份笔记，复印一份给你。你在复印件上乱涂乱画，**不会影响**我原本的那份。
    *   **编译器视角**：把实际参数的值算出来，**拷贝**到被调用函数的存储区里。
2.  **传引用/传地址 (Call by Reference)**：
    *   **比喻**：我把我家保险柜的**钥匙**给你。你打开拿走里面的钱，我再去开，钱就真的**没了**。
    *   **编译器视角**：把实际参数的**内存地址**传过去。被调用函数直接操作原来的内存区域。

---

### 第二部分：运行时存储器的划分

这一节讲的是：**编译器怎么向操作系统申请内存，并把这块内存切成不同的区域给程序用。**

#### 2.1 运行时的存储器的划分

想象操作系统给你的程序分配了一大块长条形的内存地皮。编译器通常会把它切成四块（这图你肯定见过）：

1.  **代码区 (Code)**：
    *   放编译后的机器指令。
    *   **特点**：大小固定，只读（不许改代码），也就是前面说的“静态的代码”。
2.  **静态数据区 (Static Data)**：
    *   放全局变量、常量（比如 C 里的 `static int a`）。
    *   **特点**：程序刚启动就占好坑了，直到程序结束才释放。大小也是编译时就知道的。
3.  **栈区 (Stack)**：
    *   放**函数调用**产生的数据（比如局部变量）。
    *   **特点**：像叠盘子，函数调用就压上去，函数返回就弹出来。**向低地址生长**（通常画在内存图的上方，往下长）。
4.  **堆区 (Heap)**：
    *   放你手动申请的数据（比如 C里的 `malloc`，Java/C++ 里的 `new`）。
    *   **特点**：杂乱无章，需要程序员或垃圾回收机制来管理。**向高地址生长**（通常画在内存图的下方，往上长）。

> **重点**：栈和堆中间有一块空地，如果栈长得太深（死递归），或者堆申请太多，两者撞上了，就报 `Out of Memory` 或 `Stack Overflow`。

#### 2.2 活动记录 (Activation Record / AR)

这是本章**最核心**的名词！一定要听懂。

*   **是什么？**
    还记得1.1里说的“过程的活动”吗？每次函数被调用，总得有个地方记下这个函数的参数是多少、局部变量是什么、算完后返回到哪里去吧？
    这个**记录所有信息的“小本本”**，就叫**活动记录**（也叫**栈帧** Stack Frame）。

*   **里面装了什么？**（一个典型的活动记录包含以下条目，从上到下）：
    1.  **返回值**：给调用者留的。
    2.  **实参**：调用者传进来的参数。
    3.  **控制链 (Control Link / Dynamic Link)**：指向**调用者**的活动记录的指针。（用来函数返回时恢复现场，比如 B 运行完了，得把栈顶指回 A）。
    4.  **访问链 (Access Link / Static Link)**：用来找非局部变量的（后面讲嵌套过程时是重点，先有个印象）。
    5.  **保存的机器状态**：CPU 寄存器的值（函数切换时要由CPU暂存）。
    6.  **局部变量**：函数内部定义的 `int i` 这种。
    7.  **临时变量**：计算 `(a+b)*(c+d)` 时，`a+b` 的结果需要找个地方暂存一下。

#### 2.3 存储分配策略

根据语言特性的不同，编译器会选择不同的“分蛋糕”策略：

1.  **静态分配 (Static Allocation)**：
    *   **代表**：早期的 Fortran。
    *   **逻辑**：编译的时候就算死了每个变量在哪，不需要栈，也不需要堆。
    *   **缺点**：**不支持递归**（因为递归需要同时存在两个同名函数的局部变量，静态分配只有一个坑，没法放）。
2.  **栈式分配 (Stack Allocation)**：
    *   **代表**：C, Pascal, Java (方法调用部分)。
    *   **逻辑**：也就是我们最常用的。函数调用的进出符合“后进先出”原则，非常适合用栈。
3.  **堆式分配 (Heap Allocation)**：
    *   **代表**：Lisp, Java (对象实例部分)。
    *   **逻辑**：数据的生存期和函数调用无关。比如你函数结束了，但你在函数里 `new` 出来的对象可能还得留着给别人用，这就不能放在栈里（因为栈里的东西函数一结束就弹飞了），只能放堆里。

---

### 第一讲总结（复习一下）

1.  **活动 vs 过程**：过程是死代码，活动是跑起来的一次次调用。
2.  **内存四区**：代码、静态、栈（放函数）、堆（放动态对象）。
3.  **活动记录 (AR)**：就是**栈**里的每一层，里面存了局部变量、参数和返回地址。函数调用一次，就往栈里压一个 AR。

---


### 第三部分：静态存储分配 (Static Storage Allocation)

这种分配方式在现代高级语言中作为“全盘策略”已经很少见了，但在嵌入式开发或全局变量处理中依然存在。

#### 3.1 核心概念
**“静态”**的意思是：**编译的时候**，编译器就已经把每一个变量、每一个数据要放在内存的哪个地址算得清清楚楚了。

*   **比喻**：就像是一个**“对号入座”的固定考场**。
    *   张三永远坐第1排第1座。
    *   哪怕张三今天没来考试（函数没被调用），那个座位也空着，**不许别人坐**。
    *   如果张三想把自己分身成两个（递归调用），对不起，**没地方坐**，因为只有一个属于张三的座位。

#### 3.2 适用条件与限制
要想使用全静态分配，语言必须非常“死板”，满足以下条件：
1.  **数组大小必须固定**：不能由用户输入 `n` 然后申明 `int a[n]`，因为编译器不知道 `n` 是几，就算不出大小。
2.  **不允许递归调用**：这是死穴。因为递归意味着一个函数同时存在多个“活动”，需要多个存储区，而静态分配给每个函数只留了一份地盘。
3.  **不允许动态数据结构**：不能用链表、树这种运行时才长出来的东西。

#### 3.3 FORTRAN 语言例子
早期的 FORTRAN (FORTRAN 77 以前) 就是典型的静态分配语言。
*   它的所有变量地址在程序运行前就定好了。
*   **优点**：运行速度极快！因为不需要在运行时花时间去申请内存、移动栈指针，直接读写地址就行。
*   **缺点**：写不出递归算法（比如快排、树的遍历），灵活性很差。

#### 3.4 C语言中的影子
虽然 C 语言主要使用栈式分配，但它保留了静态分配的能力。
*   **全局变量**和带有 `static` 关键字的局部变量（如 `static int count`）。
*   它们被存放在**静态数据区**。
*   它们的地址在编译时确定，且在整个程序运行期间**地址不变**。

---

### 第四部分：简单的栈式存储 (Simple Stack Allocation)

为了解决静态分配无法递归的问题，**栈 (Stack)** 登场了。

这里说的“简单”是指：**C语言这种模式**。
*   **特点**：允许递归，但**不允许**在函数里面定义函数（即没有嵌套过程）。
*   *注：Pascal 或 Ada 允许在函数 A 里定义函数 B，这叫嵌套过程，那是下一讲的难点。今天我们只看 C 语言这种平铺直叙的函数结构。*

#### 4.1 为什么要用栈？
*   **比喻**：就像**“流水席”**。
    *   张三来了（调用函数），服务员马上搬一套桌椅（活动记录 AR）放在最上面。
    *   张三还没吃完，又叫了李四（递归调用/调用子函数），服务员再搬一套桌椅摞在张三上面。
    *   李四吃完了，撤掉李四的桌椅。
    *   张三吃完了，撤掉张三的桌椅。
*   这样，哪怕是同一个函数（张三），也可以通过不断搬桌椅来同时存在多份。

#### 4.2 运行时的“指挥棒”
在栈式存储中，我们通常需要两个指针来定位：
1.  **栈顶指针 (top / sp)**：永远指向栈的最上面（下一个空闲位置）。
2.  **帧指针 (fp / frame pointer)**：指向**当前正在运行的这个函数**的活动记录的开始位置。
    *   *为什么需要fp？* 因为栈顶是变来变去的，而局部变量相对于 `fp` 的距离是固定的（编译时就算好了偏移量）。编译器生成的代码会说：“去取 `fp + 8` 位置的数据”，而不是“去取 `sp - 12` 位置的数据”。

#### 4.3 调用序列与返回序列 (Call & Return Sequences)
这是考试和理解的重点！当 `Caller`（调用者，比如main）调用 `Callee`（被调用者，比如func）时，双方要配合跳一支舞，这就叫**代码序列**。

**第一阶段：调用序列（Call Sequence）—— 建立新环境**
谁负责做什么？通常约定如下（具体细节视编译器而定，但大体逻辑一致）：

1.  **Caller 做的事**：
    *   把**实参**压入栈。
    *   把**返回地址**（Main里调用完func后该执行的下一行代码地址）压入栈。
    *   跳转到 `Callee` 的代码开始处。
2.  **Callee 做的事**（通常叫 `prologue` 序言）：
    *   **保存老的 fp**：因为我要用 `fp` 指向我自己了，但我得把 Caller 的 `fp` 存下来（放在控制链里），不然等我运行完了，Caller 找不到它的家了。
    *   **更新 fp**：让 `fp` 指向当前栈顶（或者保存的老fp位置），建立我的地盘。
    *   **分配局部变量空间**：把栈顶指针 `sp` 往上移，留出空间放局部变量。

**第二阶段：返回序列（Return Sequence）—— 恢复旧环境**
函数运行完了，要撤了：

1.  **Callee 做的事**（通常叫 `epilogue` 尾声）：
    *   把**返回值**放在某个寄存器里。
    *   **恢复 sp**：把 `sp` 降回到局部变量空间之下（回收空间）。
    *   **恢复 fp**：利用之前存的“控制链”，把 `fp` 指回 Caller 的 `fp`。
    *   跳转回返回地址。
2.  **Caller 做的事**：
    *   如果之前压入了参数，现在把 `sp` 再降一下，把参数占用的空间也吐出来（参数也不要了）。

---

### 第二讲总结（关键点复习）

1.  **静态分配**：
    *   **死板**：地址固定，大小固定。
    *   **缺点**：**不支持递归**。
    *   **例子**：Fortran，C 的 `static` 变量。
2.  **简单的栈式分配**：
    *   **灵活**：随用随开，支持递归。
    *   **机制**：利用 `sp`（栈顶）和 `fp`（帧指针）来管理。
    *   **调用/返回序列**：就像这一讲最核心的“交接仪式”，重点在于**保护现场**（存老fp）和**恢复现场**（取老fp）。

---
