## **第一讲：属性文法（Attribute Grammars）**

### 1. 什么是属性文法？

#### 1.1 文法的属性 (Attributes)
在之前的词法和语法分析中，我们只关心“符号”本身（比如 `E`, `T`, `id`）。但在语义分析中，光有一个符号是不够的，我们需要知道这个符号代表的**值**、**类型**、**位置**等信息。

*   **定义**：**属性**就是在这个文法符号上附加的额外信息。
*   **类比**：
    *   如果 `id` 是一个符号，它的属性 `id.lexeme` 可能是变量名 "count"。
    *   如果 `E` 是一个表达式，它的属性 `E.val` 可能是数值 10，`E.type` 可能是 `int`。
*   **写法**：通常用 `X.a` 来表示符号 X 的属性 a。

#### 1.2 属性翻译文法 (Attribute Grammar)
单纯的上下文无关文法（CFG）只能告诉你句子的结构（主谓宾），不能告诉你句子的意思。
**属性文法 = 上下文无关文法 + 语义规则**。

它在每一条产生式（Grammar Rule）后面，都挂了一组**公式**，用来计算属性。

*   **例子**：
    *   产生式：$E \to E_1 + T$
    *   语义规则：$E.val = E_1.val + T.val$
    *   *意思就是：父节点 E 的值，等于左孩子 E1 的值加上右孩子 T 的值。*

#### 1.3 属性依赖 (Attribute Dependence)
属性不是凭空产生的，它是由其他属性计算出来的。
*   如果 $A.x = f(B.y)$，我们就说 A 的属性 x **依赖于** B 的属性 y。
*   **依赖图**：把这些依赖关系画出来，就是一张有向图。这是为了确定计算顺序——你必须先算出原材料（B.y），才能算出产品（A.x）。

---

### 2. 核心难点：综合属性 vs 继承属性

这是考试和理解中最关键的概念，一定要分清楚数据的**流动方向**。

#### 1.4 综合属性 (Synthesized Attribute) —— “自下而上”
*   **定义**：父节点的属性值，是由**子节点**的属性值计算出来的。
*   **流动方向**：$\uparrow$（从下往上，从子到父）。
*   **直观理解**：就像**汇报工作**。底层的员工（子节点）把数据算好，层层向上汇报给领导（父节点）。
*   **典型场景**：计算表达式的值（$3+5$），必须知道 3 和 5，才能算出上层的 8。

#### 1.4 继承属性 (Inherited Attribute) —— “自上而下”或“兄弟相传”
*   **定义**：节点的属性值，是由**父节点**或者**兄弟节点**的属性值计算出来的。
*   **流动方向**：$\downarrow$（从上往下） 或者 $\rightarrow$（从左往右）。
*   **直观理解**：就像**下达命令**。领导（父节点）告诉员工（子节点）：“现在的环境是整数环境（int）”。或者哥哥（左兄弟）告诉弟弟（右兄弟）：“我已经占了第1个位置，你去第2个位置”。
*   **典型场景**：变量声明 `int a, b, c;`。`int` 这个类型信息是在最左边出现的，它需要向右传递给 `a`，再传给 `b`，再传给 `c`。

#### 1.5 语义规则 (Semantic Rules)
这就是写在产生式旁边的具体公式。
*   如果是**综合属性**，规则通常长这样：$A.s = f(Child.s)$
*   如果是**继承属性**，规则通常长这样：$Child.i = f(Parent.i, Sibling.s)$

**注意**：语义规则没有副作用（side effect），它只是纯粹的数学计算。但在实际应用中，我们有时会在规则里加 `{ print(...) }` 这样的动作，这就叫“语义动作”，后面会讲。

---

### 3. 进阶处理

#### 1.6 继承属性改编为综合属性
**为什么要改编？**
因为**自下而上**的分析器（如 LR 分析器，大家最常用的那种）处理**综合属性**非常容易（顺手就做完了）。但处理**继承属性**很麻烦，因为分析器在处理子节点时，可能还没完全搞清楚父节点的所有信息。

**怎么改编？**
这部分如果不深究，你只需要知道核心思想：**“推迟计算”** 或者 **“重写文法”**。
通常是通过改变文法的结构，消除那些需要从父节点获取信息的需求，或者把动作放到产生式的右边合适的位置，把继承属性转化为在栈中已经存在的值（综合属性）。

*(注：这部分在考试中如果只考概念，通常是问你为什么要改，或者给一个简单的例子让你手动模拟。如果不需要写复杂算法，理解到“为了适配 LR 分析器”即可。)*

---

### 第一讲总结（复习小抄）

1.  **属性**：文法符号带的变量（值、类型）。
2.  **综合属性**：$\uparrow$ 子传父（汇报）。自下而上计算。
3.  **继承属性**：$\downarrow$ 父传子 或 $\rightarrow$ 兄传弟（命令/传递）。
4.  **语义规则**：挂在产生式旁边的公式。
5.  **核心逻辑**：我们在语法树上，根据依赖关系，算出所有节点的属性值。

---
## **第二讲：基于属性文法的处理方法**

### 1. 一般的基于属性文法的处理方法 (General Methods)

这是最“笨”但也最通用的方法。不管你的属性依赖关系多么复杂（只要没有死循环），这种方法都能算出来。

*   **核心逻辑**：
    1.  先把完整的**语法树**造出来。
    2.  根据语法规则，画出所有属性的**依赖图**（A依赖B，就在图中画一条 B->A 的箭头）。
    3.  **拓扑排序**：根据依赖图决定计算顺序。简单说，就是谁不依赖别人（或者依赖的都已经算好了），就先算谁。
*   **缺点**：效率低。因为它要先把树全建好，再建图，再排序，再计算。一般编译器不会真这么干，太慢了。

---

### 2. 树遍历的属性计算方法 (Tree Traversal)

这是对上面方法的改进。我们不画复杂的依赖图了，而是规定好**怎么走这棵树**。

*   **核心逻辑**：
    1.  建立语法树。
    2.  按照预定的顺序（比如深度优先 DFS，或者从左到右）遍历这棵树。
    3.  每访问一个节点，就看看能不能计算它的属性。
*   **多遍扫描 (Multi-pass)**：
    有时候走一遍（Pass）不够。比如，有的属性需要从右边传到左边，那你第一遍从左往右走的时候就没法算。
    所以，编译器可能需要**走好几遍**这棵树。
    *   第一遍：算类型。
    *   第二遍：算地址。
    *   ...

---

### 3. 一边扫描的处理方法 (One-Pass / On-the-fly)

这是最**高效**、最**常用**的方法。

*   **核心逻辑**：
    **不显式地建立完整的语法树**，而是在**语法分析的同时**（一边读代码，一边分析结构），就把属性算出来了。
*   **举例**：
    当你用 LR 分析器（自下而上）分析 `3 + 5` 时：
    1.  读入 `3`，分析器把它归约为 $E$，顺手把 $E.val$ 设为 3。
    2.  读入 `5`，分析器把它归约为 $T$，顺手把 $T.val$ 设为 5。
    3.  读入 `+`，分析器进行 $E \to E + T$ 归约，顺手执行 $3+5=8$。
*   **限制**：
    这种方法对文法有要求（必须是 L 属性文法或 S 属性文法，后面会细讲）。你不能依赖那些“还没读进来”的信息。

---

### 4. 抽象语法树的构造 (Abstract Syntax Tree - AST)

这部分非常重要！因为**抽象语法树 (AST)** 是绝大多数现代编译器（Java, Python, GCC 等）真正工作的对象。

#### 4.1 语法树 (Parse Tree) vs 抽象语法树 (AST)
*   **语法树（CST）**：是**具体**的。它包含了很多为了推导而存在的“废话”节点。
    *   比如 `E -> T`, `T -> F`, `F -> id`。为了表示一个 `x`，树上挂了一串单链 `E-T-F-x`。
    *   还包括了括号 `(`, `)`，分号 `;` 等在逻辑上不需要的符号。
*   **抽象语法树（AST）**：是**提纯**的。它只保留逻辑结构。
    *   不包含括号（树的层级本身就代表了优先级）。
    *   不包含多余的单链推导。
    *   **节点即运算**：AST 的父节点通常就是操作符（`+`, `assign`, `if`），子节点就是操作数。

#### 4.2 怎么构造 AST？
构造 AST 本身就是一个**语法制导翻译**的过程！
我们把“构造节点”写成语义规则。

*   **函数**：通常定义特定的函数来造树，比如 `mknode(op, left, right)` 和 `mkleaf(id, entry)`。
*   **例子**：
    *   产生式：$E \to E_1 + T$
    *   语义规则：$E.ptr = mknode('+', E_1.ptr, T.ptr)$
    *   *解释：E 的属性 ptr 指向一个新造的节点，这个节点的左孩子是 E1 指向的树，右孩子是 T 指向的树。*

最终，属性 $E.ptr$ 指向的就是这棵构建好的 AST 的根节点。

---

### 第二讲总结（复习小抄）

1.  **一般方法**：建树 -> 依赖图 -> 拓扑排序 -> 计算。最稳但最慢。
2.  **树遍历**：建树 -> 多遍(Pass)遍历。
3.  **一遍扫描**：**最重要**。边分析语法，边计算属性。不存完整的树，效率最高。
4.  **AST**：**精华版**的语法树。去掉了括号和冗余推导，只留操作符和操作数。
5.  **构造 AST**：利用**综合属性**（自下而上），通过 `mknode` 函数把子树拼成大树，指针层层上传。

---
## **第三讲：S 属性文法的自下而上计算**


### 1. 什么是 S 属性文法 (S-Attributed Definition)？

名字里的 **"S"** 代表 **Synthesized**（综合属性）。

*   **定义**：如果一个属性文法中，**所有的属性都是综合属性**，那它就是 S 属性文法。
*   **特点**：数据流向极其单纯，只有 **$\uparrow$（自下而上）**。
    *   不需要从父节点拿数据，也不需要从左兄弟拿数据。
    *   一切都等着子节点算好，汇报上来即可。

**为什么它很重要？**
因为它和 **LR 分析（自下而上分析）** 是天作之合！
LR 分析的过程是“归约”（Reduce），也就是把孩子节点（右部）替换成父节点（左部）。这正好对应综合属性的计算时机——孩子都齐了，正好计算父节点的值。

---

### 2. 改造 LR 分析器

要让原本只懂语法的 LR 分析器学会算数，我们需要对它进行简单的“硬件升级”和“软件升级”。

#### 2.1 硬件升级：扩充分析栈 (The Value Stack)
标准的 LR 分析器里有一个**状态栈**，用来记录解析到了哪里。
现在，我们需要加一个**语义栈**（或者叫值栈，Value Stack）。

*   **状态栈**：存分析状态（$0, 1, 5, \dots$）。
*   **语义栈**：存在这个状态下对应的属性值（$val$）。这两个栈是**同步**生长的。

**图解**：
假设正在分析 `3 + 5`。
*   状态栈：`[ ... state_E, state_+, state_T ]`
*   语义栈：`[ ... 3,       null,    5       ]`
*(注：`+` 号通常没有值，或者是操作符本身，这里简化理解)*

#### 2.2 软件升级：扩充总控程序的功能
原本 LR 分析器在遇到 **“归约” (Reduce)** 动作时，只是把栈里的符号弹出来，换成左部符号压进去。
现在，在归约的同时，我们要执行 **语义动作**。

**具体流程**：
假设产生式是 $A \to X Y Z$，语义规则是 $A.val = f(X.val, Y.val, Z.val)$。

1.  **弹出**：分析器准备归约，要从状态栈弹出 3 个状态（对应 X, Y, Z）。
2.  **计算**：同时，从**语义栈**里取出顶部的 3 个值（对应 $Z.val, Y.val, X.val$）。
3.  **执行**：把这 3 个值带入公式 $f(\dots)$，算出结果。
4.  **压栈**：把算出来的 $A.val$ 压入语义栈，同时把状态栈压入新状态。

---

### 3. 举个栗子 (手动模拟)

假设我们要计算 `3 * 5 + 4`。
文法：
1.  $E \to E + T$  `{ E.val = E1.val + T.val }`
2.  $E \to T$      `{ E.val = T.val }`
3.  $T \to T * F$  `{ T.val = T1.val * F.val }`
4.  $T \to F$      `{ T.val = F.val }`
5.  $F \to digit$  `{ F.val = digit.lexval }`

**分析过程片段**：

1.  **读入 3**：归约 $F \to 3$。
    *   语义栈操作：把 `3` 压入。
    *   栈：`[3]`
2.  **归约 T -> F**：
    *   语义栈操作：取出 `3`，计算 `T.val = 3`，压回。
    *   栈：`[3]` （此时代表 T 的值）
3.  **读入 *, 读入 5**：归约 $F \to 5$。
    *   语义栈操作：压入 `5`。
    *   栈：`[3, 5]` （栈底是 T，栈顶是 F）
4.  **归约 T -> T * F**：
    *   语义栈操作：弹出 `5` (F.val) 和 `3` (T.val)。
    *   计算：`3 * 5 = 15`。
    *   压入 `15`。
    *   栈：`[15]` （此时代表 T 的值）
5.  ...后续同理，遇到 + 4 再算加法。

---

### 第三讲总结（复习小抄）

1.  **S 属性文法**：**纯综合属性**（Pure Synthesized）。只有 $\uparrow$ 数据流。
2.  **黄金搭档**：S 属性文法 + LR 分析器（Bottom-Up）。
3.  **栈的扩充**：增加一个**语义栈**，和状态栈“同进同出”。
4.  **执行时机**：每次 **归约 (Reduce)** 时，利用语义栈里刚刚处理完的子节点的值，计算父节点的值，然后替换栈顶。

---

## **第四讲：L 属性定义与翻译模式**

### 1. 什么是 L 属性定义 (L-Attributed Definition)？

S 属性文法虽然简单（只向上汇报），但它做不了**继承属性**（从上往下传、从左往右传）。比如“变量声明”时，类型信息 `int` 在最左边，必须传给右边的变量 `a, b, c`。这就需要 **L 属性文法**。

#### 1.1 名字的含义
*   **"L"** 代表 **Left-to-right**（从左到右扫描）。
*   它的核心思想是：我们在分析输入串时，是**从左向右**读的，所以数据的计算也应该顺应这个方向。

#### 1.2 核心限制规则
在一个产生式 $A \to X_1 X_2 \dots X_n$ 中，如果我们要计算右边某个符号 $X_i$ 的**继承属性**，我们只能依赖：
1.  **父节点 A** 的继承属性（上一级传下来的）。
2.  **左边兄弟** ($X_1 \dots X_{i-1}$) 的属性（已经处理过的兄弟）。
3.  **绝对不能依赖右边兄弟**！因为从左往右扫描时，右边的兄弟还没读进来呢，不知道它们是谁。

*一句话总结：L 属性文法允许数据 $\uparrow$（向上），$\downarrow$（向下），$\rightarrow$（向右），但禁止 $\leftarrow$（向左）。*

---

### 2. 翻译模式 (Translation Schemes)

这是这一节最“像代码”的部分。

#### 2.1 什么是翻译模式？
之前我们把语义规则写在产生式的**最后面**。
但在 L 属性文法中，因为数据要从左往右流，有时候我们需要在产生式的**中间**就把动作做完。

**翻译模式**就是把**语义动作（Semantic Actions）**（通常是用花括号 `{}` 包起来的代码段）插入到产生式右部的**任何位置**。

*   **例子**：
    *   产生式：$S \to T \ R$
    *   翻译模式：$S \to T \ \{ print('+'); \} \ R$
    *   *含义：先分析 T，然后打印一个加号，然后再分析 R。*

#### 2.2 为什么要插在中间？
为了处理**继承属性**。
举个声明变量的例子：`int a, b;`
文法可能是 $D \to T \ L$（$T$ 是类型，$L$ 是变量列表）。
我们需要算出 $T.type$（比如 int），然后立刻把它存到一个全局变量里，或者传给 $L$。如果不插在中间，等 $L$ 全部分析完了再回过头来找 $T$，可能就晚了（在某些分析策略下）。

**典型写法**：
$D \to T \ \{ L.in = T.type \} \ L$
(意思是：解析完 T 后，立刻把 T 的类型赋值给 L 的继承属性，然后再去解析 L)。

---

### 3. 属性文法 vs 翻译模式 (The Difference)

这是考试常考点，也是概念上的分水岭。

| 比较维度 | 属性文法 (Attribute Grammar) | 翻译模式 (Translation Scheme) |
| :--- | :--- | :--- |
| **本质** | **声明式 (Declarative)** | **过程式 (Procedural)** |
| **关注点** | 关注 **“是什么”** (What) | 关注 **“什么时候做”** (When) |
| **形式** | 类似于数学公式，没有顺序概念。<br>$E.val = E_1.val + T.val$ | 嵌入了动作的代码片段，有严格顺序。<br>$E \to E_1 + T \{ print('+') \}$ |
| **执行** | 不指定计算顺序，只要依赖关系满足即可。 | 必须按照分析器解析符号的顺序（从左到右）显式执行动作。 |

*   **比喻**：
    *   **属性文法**是**菜谱的配料表**：“番茄炒蛋 = 番茄 + 蛋”。它没告诉你先放谁。
    *   **翻译模式**是**烹饪步骤**：“切番茄 -> 打蛋 -> 炒蛋 -> 放番茄”。顺序错了就做不出来了。

---

### 4. 怎么实现 L 属性文法？ (S + L 的大一统)

在实际编译器（如 GCC, LLVM 前端）中，我们通常结合使用：

1.  **如果是自顶向下分析（LL）**：
    非常简单！写递归函数时，把**继承属性**作为**函数参数**传进去，把**综合属性**作为**返回值**拿出来。
    ```c
    // 伪代码: D -> T L
    void parse_D() {
        Type t = parse_T(); // 综合属性：从子节点拿回来
        parse_L(t);         // 继承属性：作为参数传给子节点
    }
    ```

2.  **如果是自下而上分析（LR）**：
    这就有点麻烦，因为 LR 是“后序遍历”（先孩子后父亲）。
    为了支持“中途执行动作”，我们通常会在文法里塞入**标记非终结符 (Marker Non-terminal)**。
    *   原版：$A \to b \{ action \} c$
    *   改造后：
        $A \to b \ M \ c$
        $M \to \epsilon \ \{ action \}$
    这样分析器归约 $M \to \epsilon$ 时，就会触发动作。虽然稍微麻烦点，但能解决问题。

---

### 第四讲总结（复习小抄）

1.  **L 属性定义**：允许继承属性，但限制依赖方向。只能依赖**父节点**或**左兄弟**（Left-to-right flow）。
2.  **S 是 L 的特例**：S 属性文法只允许综合属性，它当然符合 L 的规则（毕竟不需要左兄弟）。
3.  **翻译模式**：把语义动作 `{ code }` 插入到产生式右部的**任意位置**，明确规定了动作的**执行时机**。
4.  **区别**：属性文法是“定义依赖关系（数学公式）”，翻译模式是“定义执行步骤（代码指令）”。

---


